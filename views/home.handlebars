<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sarah White Portfolio</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <!-- Bio Section -->
  <div class="container mt-4">
    <div class="card p-3 mb-4">
      <h5 class="card-title text-primary">About Me</h5>
      <p>Hi, I'm <strong>Sarah White</strong>, a passionate artist who loves working with color and texture. My portfolio explores the emotions and energy found in every color palette. Each piece tells a story through vibrant, textured visual art.</p>
    </div>

    <!-- Recent Projects -->
    <h2 class="mb-3">{{pageTitle}}</h2>
    <div class="row">
      {{#each projects}}
        <div class="col-md-4 mb-4">
          <div class="card h-100 shadow-sm">
            <img src="/{{encodeURIComponent image}}" class="card-img-top" alt="{{title}}">
            <div class="card-body">
              <h5 class="card-title">{{title}}</h5>
              <p class="card-text">{{description}}</p>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>

</body>
</html>